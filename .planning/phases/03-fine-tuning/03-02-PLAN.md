---
phase: 03-fine-tuning
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified: [src/hcaptcha_challenger/agent/robotic.py, src/hcaptcha_challenger/agent/config.py, examples/demo_humancursor_adapter.py]
autonomous: true
requirements: [PH3-DEL, PH3-TEL]

must_haves:
  truths:
    - "RoboticArm selects a random persona on initialization"
    - "Recognition delays are applied after AI model inference"
    - "Success/failure outcomes are logged with persona information"
  artifacts:
    - path: "src/hcaptcha_challenger/agent/robotic.py"
      provides: "Integrated persona behavior and delays"
    - path: "src/hcaptcha_challenger/agent/config.py"
      provides: "Telemetry storage configuration"
    - path: "tmp/.telemetry/telemetry.json"
      provides: "Empirical calibration data"
  key_links:
    - from: "src/hcaptcha_challenger/agent/robotic.py"
      to: "telemetry.json"
      via: "Success tracking logs"
---

<objective>
Integrate the Persona system and Recognition Delays into the `RoboticArm` workflow and implement basic telemetry for empirical calibration.

Purpose: Synchronize agent behavior with human-like cognitive delays and track effectiveness.
Output: Integrated `robotic.py` and a functional telemetry system.
</objective>

<execution_context>
@D:/hcaptcha-challenger/.gemini/get-shit-done/workflows/execute-plan.md
@D:/hcaptcha-challenger/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fine-tuning/03-RESEARCH.md
@.planning/3-CONTEXT.md
@src/hcaptcha_challenger/agent/robotic.py
@src/hcaptcha_challenger/agent/config.py
@examples/demo_humancursor_adapter.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Persona and Recognition Delay</name>
  <files>src/hcaptcha_challenger/agent/robotic.py, src/hcaptcha_challenger/agent/config.py</files>
  <action>
    - Add `telemetry_dir: Path = Path("tmp/.telemetry")` to `AgentConfig` in `src/hcaptcha_challenger/agent/config.py`.
    - Update `RoboticArm.__init__` in `src/hcaptcha_challenger/agent/robotic.py` to use `select_random_persona()`.
    - Store the selected persona name in `self._persona_name`.
    - Insert `sleep_ms(rand_range(self._human_cfg.recognition_delay))` after AI classifier/reasoner calls in:
        - `challenge_image_label_binary`
        - `challenge_image_drag_drop`
        - `challenge_image_label_select`
    - Log the selected persona at INFO level on init.
  </action>
  <verify>
    <automated>grep -E "recognition_delay|select_random_persona" src/hcaptcha_challenger/agent/robotic.py</automated>
  </verify>
  <done>Persona integrated and delays applied.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Basic Telemetry</name>
  <files>src/hcaptcha_challenger/agent/robotic.py</files>
  <action>
    - Implement a method `_record_outcome(success: bool)` in `RoboticArm` that appends a record to `tmp/.telemetry/telemetry.json`.
    - Record format: `{"timestamp": "...", "persona": "...", "success": true/false}`.
    - Call this method at the end of each `challenge_*` method based on the return value.
  </action>
  <verify>
    <automated>python -c "import os; print(os.path.exists('tmp/.telemetry/telemetry.json') or 'File will be created on first run')"</automated>
  </verify>
  <done>Telemetry logging implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Update Visual Demo for Personas</name>
  <files>examples/demo_humancursor_adapter.py</files>
  <action>
    - Update `examples/demo_humancursor_adapter.py` to optionally accept a persona name or show multiple personas in sequence.
    - Ensure it demonstrates the variations in speed and jitter between personas.
  </action>
  <verify>
    <automated>python examples/demo_humancursor_adapter.py --help</automated>
  </verify>
  <done>Demo script updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Agent -> Local Storage | Writing telemetry data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | telemetry.json | accept | Internal log file, low risk. |
</threat_model>

<verification>
Run `python examples/demo_humancursor_adapter.py` and verify visual differences. Check `tmp/.telemetry/telemetry.json` after running tests or the demo to confirm data is recorded.
</verification>

<success_criteria>
- RoboticArm uses personas and honors recognition delays.
- Telemetry data is recorded correctly.
- Demo script provides visual confirmation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fine-tuning/03-02-SUMMARY.md`
</output>
