---
phase: 03-fine-tuning
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/hcaptcha_challenger/agent/mouse_config.py, tests/test_mouse_config.py]
autonomous: true
requirements: [PH3-PER]

must_haves:
  truths:
    - "HumanConfig includes recognition_delay range"
    - "Standard, Hesitant, and Fast persona presets are defined"
    - "A weighted random persona selection function exists"
  artifacts:
    - path: "src/hcaptcha_challenger/agent/mouse_config.py"
      provides: "Persona system and behavior presets"
    - path: "tests/test_mouse_config.py"
      provides: "Unit tests for persona resolution"
  key_links:
    - from: "src/hcaptcha_challenger/agent/mouse_config.py"
      to: "HumanConfig"
      via: "Dataclass extension"
---

<objective>
Implement a Multi-Profile Persona system in `mouse_config.py` to vary agent behavior and avoid statistical detection.

Purpose: Introduce behavioral diversity (Standard, Hesitant, Fast) and model "Human Thinking Time".
Output: Refined `mouse_config.py` with persona presets and selection logic.
</objective>

<execution_context>
@D:/hcaptcha-challenger/.gemini/get-shit-done/workflows/execute-plan.md
@D:/hcaptcha-challenger/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fine-tuning/03-RESEARCH.md
@.planning/3-CONTEXT.md
@src/hcaptcha_challenger/agent/mouse_config.py
@tests/test_mouse_config.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend HumanConfig and Define Personas</name>
  <files>src/hcaptcha_challenger/agent/mouse_config.py</files>
  <behavior>
    - Add `recognition_delay: Range = (200, 500)` to `HumanConfig`.
    - Implement `_standard_config()`, `_hesitant_config()`, and `_fast_config()` as presets.
    - Standard: balanced speed and jitter.
    - Hesitant: slower (`mouse_speed=1.8`), more knots (`knots_count=4`), longer delay (`recognition_delay=(500, 1200)`).
    - Fast: faster (`mouse_speed=0.6`), fewer knots (`knots_count=1`), shorter delay (`recognition_delay=(150, 300)`).
  </behavior>
  <action>
    Update `HumanConfig` dataclass in `src/hcaptcha_challenger/agent/mouse_config.py` to include `recognition_delay`. 
    Implement the persona preset functions as per research recommendations.
    Update `_PRESETS` dictionary to include the new personas.
  </action>
  <verify>
    <automated>python -c "from hcaptcha_challenger.agent.mouse_config import HumanConfig; print(HumanConfig())"</automated>
  </verify>
  <done>HumanConfig updated and presets defined.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Weighted Persona Selection</name>
  <files>src/hcaptcha_challenger/agent/mouse_config.py</files>
  <action>
    Implement `select_random_persona() -> HumanConfig` in `src/hcaptcha_challenger/agent/mouse_config.py`.
    Use weighted probabilities: 60% Standard, 20% Fast, 20% Hesitant (per research).
    The function should return a fully resolved `HumanConfig` instance.
  </action>
  <verify>
    <automated>python -c "from hcaptcha_challenger.agent.mouse_config import select_random_persona; print(select_random_persona())"</automated>
  </verify>
  <done>Weighted selection logic implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update Mouse Config Tests</name>
  <files>tests/test_mouse_config.py</files>
  <behavior>
    - Test that `select_random_persona()` returns a valid `HumanConfig`.
    - Test that `resolve_config()` correctly handles new persona names.
    - Verify `recognition_delay` is present in resolved configs.
  </behavior>
  <action>
    Update `tests/test_mouse_config.py` to cover the new persona system and the `recognition_delay` field.
  </action>
  <verify>
    <automated>pytest tests/test_mouse_config.py</automated>
  </verify>
  <done>Tests updated and passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Config -> Agent | Internal parameters trust boundary |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Information Disclosure | mouse_config.py | mitigate | Use randomization (Personas) to prevent behavioral fingerprinting by hCaptcha. |
</threat_model>

<verification>
Run `pytest tests/test_mouse_config.py` to ensure all presets are correctly loaded and randomized selection works.
</verification>

<success_criteria>
- Persona presets are implemented and weighted selection is functional.
- Unit tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/03-fine-tuning/03-01-SUMMARY.md`
</output>
