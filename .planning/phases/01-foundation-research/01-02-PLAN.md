---
phase: 01-foundation-research
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [prototype_trajectory_comparison.py]
autonomous: false
requirements: [FR2, NFR1, NFR2]
must_haves:
  truths:
    - "A prototype script exists that compares old vs new trajectories"
    - "The script generates a matplotlib plot for visual validation"
    - "New trajectories are confirmed to be human-like (non-linear, micro-jitter)"
  artifacts:
    - path: "prototype_trajectory_comparison.py"
      provides: "Visualization tool for trajectory analysis"
  key_links:
    - from: "prototype_trajectory_comparison.py"
      to: "src/hcaptcha_challenger/agent/mouse.py"
      via: "import generate_human_trajectory"
---

<objective>
Create a prototype script to visually compare the old (pre-refactor) and new (post-refactor) trajectory generation logic. This serves as a qualitative verification of the "human-likeness" of the new engine.

Purpose: Visual validation of trajectory quality.
Output: Comparison plot and validation findings.
</objective>

<execution_context>
@D:/hcaptcha-challenger/.gemini/get-shit-done/workflows/execute-plan.md
@D:/hcaptcha-challenger/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-research/01-01-SUMMARY.md
@src/hcaptcha_challenger/agent/mouse.py

<interfaces>
From src/hcaptcha_challenger/agent/mouse.py:
```python
def generate_human_trajectory(from_pt, to_pt, steady=False):
    # Returns List[Tuple[float, float]]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create prototype_trajectory_comparison.py</name>
  <files>prototype_trajectory_comparison.py</files>
  <action>
    Create a script that:
    1. Re-implements the "old" trajectory logic (as a standalone function `generate_old_trajectory`). You can find this logic in the git history or from the `01-01` plan's context (the inlined math version).
    2. Imports the "new" `generate_human_trajectory` from `hcaptcha_challenger.agent.mouse`.
    3. Defines a set of test paths (e.g., (0,0) to (500,500), (100,500) to (400,100)).
    4. Uses `matplotlib` to plot both trajectories for each path on the same figure (different colors/styles).
    5. Saves the plot to `trajectory_comparison.png`.
  </action>
  <verify>
    <automated>python prototype_trajectory_comparison.py</automated>
  </verify>
  <done>Script runs and generates `trajectory_comparison.png`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>A visualization script for comparing mouse trajectories.</what-built>
  <how-to-verify>
    1. Open `trajectory_comparison.png`.
    2. Observe the "Old" vs "New" paths.
    3. Verify that the new paths (from `humancursor`) exhibit human-like traits:
       - Non-linear curves (Bezier)
       - Micro-jitter (Gaussian distortion)
       - Varying point density (Easing)
    4. Confirm that the new paths are at least as "natural" as the old ones.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
N/A (Prototype script, local execution only)
</threat_model>

<verification>
Manual review of the generated plots.
</verification>

<success_criteria>
- `trajectory_comparison.png` is generated.
- The user visually confirms the quality of the new trajectories.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-research/01-02-SUMMARY.md`
</output>
