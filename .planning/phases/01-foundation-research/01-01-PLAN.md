---
phase: 01-foundation-research
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/hcaptcha_challenger/agent/mouse.py, tests/test_mouse_trajectory.py]
autonomous: true
requirements: [FR1, FR3, FR4, NFR3]
must_haves:
  truths:
    - "mouse.py uses humancursor and pytweening instead of inlined math"
    - "human_move implementation is compatible with DrissionPageMouse adapter"
    - "Unit tests verify trajectory generation and point boundaries"
  artifacts:
    - path: "src/hcaptcha_challenger/agent/mouse.py"
      provides: "Refactored mouse movement logic"
    - path: "tests/test_mouse_trajectory.py"
      provides: "Automated verification for mouse trajectories"
  key_links:
    - from: "src/hcaptcha_challenger/agent/mouse.py"
      to: "humancursor"
      via: "import HumanizeMouseTrajectory, BezierCalculator"
    - from: "tests/test_mouse_trajectory.py"
      to: "src/hcaptcha_challenger/agent/mouse.py"
      via: "import generate_human_trajectory"
---

<objective>
Refactor the core mouse movement logic to use the `humancursor` library and `pytweening` for easing. This replaces ~200 lines of inlined mathematical logic with a standardized, maintainable dependency.

Purpose: Improve trajectory realism and maintainability.
Output: Refactored `mouse.py` and a new test suite.
</objective>

<execution_context>
@D:/hcaptcha-challenger/.gemini/get-shit-done/workflows/execute-plan.md
@D:/hcaptcha-challenger/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-research/01-CONTEXT.md
@.planning/phases/01-foundation-research/01-RESEARCH.md
@src/hcaptcha_challenger/agent/mouse.py
@src/hcaptcha_challenger/agent/robotic.py

<interfaces>
From humancursor:
```python
from humancursor.utilities.human_curve_generator import HumanizeMouseTrajectory, BezierCalculator
# HumanizeMouseTrajectory(from_pt, to_pt).points -> List[Tuple[float, float]]
```

From pytweening:
```python
import pytweening
# pytweening.easeOutQuad(t), etc.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create baseline tests for trajectory generation</name>
  <files>tests/test_mouse_trajectory.py</files>
  <action>
    Create a test file to verify the current and future `generate_human_trajectory` behavior.
    - Test that `generate_human_trajectory` returns a list of points.
    - Test that the first point is approximately the start and the last point is the target.
    - Test that it handles small distances (dist < 1).
    - Test that point counts are within reasonable bounds (e.g., 2 to 150 points).
  </action>
  <verify>
    <automated>pytest tests/test_mouse_trajectory.py</automated>
  </verify>
  <done>Baseline tests pass with current inlined logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor mouse.py to use humancursor</name>
  <files>src/hcaptcha_challenger/agent/mouse.py</files>
  <behavior>
    - `generate_human_trajectory` must use `HumanizeMouseTrajectory` from `humancursor`.
    - Inlined `_binomial`, `_bernstein_polynomial`, `_calculate_bezier_points`, and `_EASING_FUNCTIONS` must be removed.
    - `human_move` must maintain compatibility with `RawMouse` protocol.
    - `AgentConfig.MOUSE_SPEED` must be integrated into the movement loop delay.
  </behavior>
  <action>
    1. Import `HumanizeMouseTrajectory` and `BezierCalculator` from `humancursor.utilities.human_curve_generator`.
    2. Import `pytweening`.
    3. Delete inlined math functions and the `_EASING_FUNCTIONS` list.
    4. Update `generate_human_trajectory` to wrap `HumanizeMouseTrajectory`.
    5. Update `human_move` loop:
       - Use the points from `generate_human_trajectory`.
       - Apply `sleep_ms(rand_range(cfg.mouse_move_delay_ms) * speed)` per point.
       - Retain burst logic for realism.
    6. Update `human_click` to use `humancursor` style clicking if possible, or keep existing if better suited for DrissionPage. (per D-02: browser actions handle real interaction).
  </action>
  <verify>
    <automated>pytest tests/test_mouse_trajectory.py</automated>
  </verify>
  <done>Code is refactored and tests pass using the new library.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent -> Library | External humancursor library generates coordinates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | mouse.py | mitigate | Ensure coordinate inputs are validated/bounded before passing to library. |
| T-01-02 | DoS | mouse.py | mitigate | Prevent excessive point generation for very long paths by capping target_points. |
</threat_model>

<verification>
- Run `pytest tests/test_mouse_trajectory.py` to ensure mathematical validity.
- Run `pytest tests/test_tools_common.py` to ensure no regressions in common tools.
</verification>

<success_criteria>
- `mouse.py` imports `humancursor` and `pytweening`.
- No `_binomial` or `_bernstein_polynomial` functions remain in `mouse.py`.
- `generate_human_trajectory` works as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-research/01-01-SUMMARY.md`
</output>
